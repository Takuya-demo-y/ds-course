# ============================================================
# データサイエンス実践コース - 必要ライブラリ一覧
# Python 3.11.9 対応
# ============================================================

# --- データ操作 ---
numpy==1.26.4
pandas==2.2.2

# --- 可視化 ---
# ※ japanize-matplotlib==1.1.3（最終更新2020年）との互換性のため 3.9.4 を使用。
#    3.10.0 では日本語フォント設定が正常に動作しない可能性があります。
matplotlib==3.9.4
seaborn==0.13.2
japanize-matplotlib==1.1.3

# --- 画像処理 ---
# ※ Ch.3「画像処理入門」で PIL（Pillow）を使用。必須。
Pillow==10.3.0

# --- 機械学習 ---
scikit-learn==1.5.0
scipy==1.15.3

# --- Jupyter 環境 ---
ipykernel==6.29.5
jupyter==1.1.1

# ============================================================
# 以下は本コースでは使用しないため削除済み
# （必要な場合は別途インストールしてください）
# ============================================================
# openpyxl       → Excelファイル操作用。本コースはscikit-learn内蔵データのみ使用
# xgboost        → 本コースはRandomForestのみ使用
# imbalanced-learn → 不均衡データ対策。本コースのスコープ外
# optuna         → 自動ハイパーパラメータ最適化。本コースは手動チューニングのみ
# statsmodels    → 統計モデリング。本コースでは使用しない
# tensorflow     → 深層学習。本コースはscikit-learnのみ。インストールに500MB超かかる
# ipywidgets     → インタラクティブWidget。本コースのNotebookでは使用しない
